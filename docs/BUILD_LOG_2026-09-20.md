# Continuing build log

## Identity exceptions

Added a shared identity-review classification to the scanner card, intake draft, report and history. Failed analysis, unknown category, limited confidence, requested supporting evidence and a wrong rating produce an explicit unresolved identity. A helpful rating and high confidence do not confirm identity. A recorded human name with identity evidence is labeled as a human record, not independent verification or safety certification. History can filter unresolved identities without changing training labels.

Verification: initial classification test failed before implementation; then 97 focused tests passed across classification, scanner, result, history and report. Added a history-filter regression test and report/result assertions; the affected 50 tests passed (98 unique tests across the final suites). Lint and production build passed; existing large-bundle warning remains. QA doctor and mobile-emulated auth-login, saved-history and result-detail passed. Evidence: `artifacts/qa/2026-09-20T18-11-50-053Z/report.md` and its screenshots/trace/video. History screenshot visually checked. No new backend schema or provider request is introduced.

At this checkpoint, guided retakes and rejected-photo preservation were pending; see the completed increment below. Existing cloud inspection migration remains unapplied. The history screenshot also exposes an existing dark heading on a dark page background; fix and verify that contrast in a subsequent bounded change.

## Rejected photos and one guided retake

Quality-rejected camera/upload photos now save through the normal record path with `quality_rejected`, an actionable reason and a failed quality snapshot. AI identification is not invoked for these images. The scanner offers one guided retake for photo-quality failures or AI requests for a better photo/another angle. Camera users return to the live view to adjust before capturing; upload users choose a replacement file. Canceling the file chooser does not spend the retake. After a second insufficient result, the guided invitation stops and the record remains unresolved. Original and retake captures remain separate records rather than overwriting evidence. Closing the review explicitly starts a separate sequence.

The cap is per active scanner sequence, not a durable quota across reloads or devices. There is no background retry. The reason and exhausted outcome of a quality rejection survive reload and text export. Retake lineage is not yet a structured cross-device field; do not count two attempts as two evaluated physical parts. Local storage still has its existing 50-scan cap and quota failure handling. Cloud inspection schema limitations remain separate.

Verification: new camera tests failed before implementation. Final scanner/storage/report/classification suites passed 95 tests; result/report regression run passed 40 (122 unique tests across these suites). Tests cover failed and successful camera retakes, preserved original evidence, upload cancellation, repeated rejected uploads and AI-requested photo evidence. Lint and production build passed with the existing bundle-size warning. QA doctor passed. Browser production-build run `artifacts/qa/2026-09-20T18-21-19-663Z/report.md` passed auth-login and the new explicit `scanner-quality-retake` scenario: two generated unusable uploads, zero identify requests, both records retained, reload and actual downloaded report verified. Earlier screenshot `artifacts/qa/2026-09-20T18-19-51-993Z/screenshots/quality-retake-exhausted.png` was visually inspected. A generic retry instruction discovered in the downloaded report was reproduced in a failing test and corrected before the final run. Cloud-write completion and physical camera behavior were not asserted by this scenario.

## Next work order

1. Completed: preserve quality-check captures and bound the guided retake invitation for the active sequence.
2. Completed locally: verify saved/reloaded/exported quality exceptions. Cross-device verification remains a separate rollout gate.
3. Completed: fixed the observed history heading contrast (see below).
4. Completed: pilot measurement packet, empty JSON template and tested local summary tool; no customer measurements supplied.
5. Continue only with work that is testable locally. Physical-part trials, buyer commitments, production migration and cross-account checks require their actual external prerequisites.

## History heading contrast

The saved-history heading inherited dark card text on the dark page background. Applied the existing page foreground token only to that heading. The screenshot now shows a readable heading while cards retain their original text styles. Added a browser contrast check using canvas color conversion so computed OKLCH and RGB colors are measured consistently. The first check's numeric output was invalid for OKLCH and is not used as a before/after measurement. Final measured contrast: 20.16:1.

Verification: production build and scoped lint passed. QA doctor and mobile auth-login/saved-history passed in `artifacts/qa/2026-09-20T18-23-56-277Z/report.md`; its saved-history screenshot was visually checked. No new unit test was needed for this one-line color change; the browser check exercises the rendered colors.

## Pilot measurement packet

Added `docs/PILOT_RUNBOOK.md`, an empty JSON observation template and `scripts/pilot/summarize-intake-pilot.mjs`. The read-only local command keeps batches and workflow arms separate; reports all attempted/completed/abandoned and identity counts; retains functional-test time in active time; and rejects invalid timings, missing evidence for correctness judgments, or duplicate physical parts. Time reductions are descriptive, not a launch gate. Missing measurements must remain in the raw narrative log; the summary covers only supplied valid measured rows. A separate ignored `artifacts/pilot/` directory protects working study data from routine commits.

Verification: 14 tests passed, including failures/unknowns in denominators, batch separation, invalid input and duplicate-part rejection. Scoped lint passed. CLI invocation against the empty template returned `no_observations`; missing input returned usage with exit code 1 as intended. The template contains no synthetic customer results. No network, buyer contact, payment or production code change is involved. The participating business, physical parts, timings, reference judgments, service costs and paid commitment remain unavailable and must be supplied by real observation.

## Live account and private-image isolation

Ran the existing Supabase verifier with fresh QA accounts and generated sentinel rows/images. Owner save/read of two scans and related candidate/evidence/correction/model-run/sync-event rows passed; the second account could not read those sentinel records. Extended the verifier to require explicit access denial when the second account downloads each private image, after successful owner downloads established object existence. Network failures, expired auth and generic bad requests are inconclusive rather than privacy passes.

Verification: eight denial-classification tests and scoped lint passed. Live baseline and extended checks both passed; logs are `artifacts/qa/cloud-isolation-2026-09-20/baseline.txt` and `private-images.txt`. The verifier's cleanup is scoped to its generated account/record IDs and image paths; it does not target pre-existing user data. Cleanup return values are not currently asserted, so no complete-cleanup guarantee is made. No secrets are in these logs or commits.

Scope limits: these checks establish the exercised database rows/private-image reads for two generated anonymous accounts, not organization roles, signed-link redistribution, all write policies, browser-local account switching, or inspection cloud save/reload. The existing inspection migration remains unapplied. Source review found that sign-out clears auth cache but not device scan history; investigate and reproduce the browser-local boundary next before claiming shared-device isolation.
